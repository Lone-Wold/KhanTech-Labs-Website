/*
 * KhanTech Labs site configuration.
 *
 * App store links. Leave both empty until the app has a real, public store
 * listing: while a value is empty, its download button is not shown at all.
 *
 * When a real listing exists, paste its full URL between the quotes, e.g.
 *   GOOGLE_PLAY_URL: 'https://play.google.com/store/apps/details?id=...'
 *   APPLE_APP_STORE_URL: 'https://apps.apple.com/za/app/...'
 * and the matching button appears on the home page automatically. Only https
 * links on play.google.com (Google Play) and apps.apple.com / itunes.apple.com
 * (App Store) are accepted; anything else is ignored.
 *
 * Also update the "Mobile app" wording on index.html at that point: it currently
 * says the app is in private pilot testing.
 */
window.KHANTECH_CONFIG = {
  GOOGLE_PLAY_URL: '',
  APPLE_APP_STORE_URL: ''
};
